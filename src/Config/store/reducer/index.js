const Initial_State ={
    name: "Saqib"
}

export default(state = Initial_State, action)=>{
    // switch(action.type){
    //     case 'SETDATA':
    //         return({
    //             ...state,
    //             name: [...state.name, action.payload]
    //         })
    // }
    return state
}